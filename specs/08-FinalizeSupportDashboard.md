## FINALIZE SUPPORT DASHBOARD (`team-support-dashboard.html`).
 # NEW FUNTIONALITY TO BE ADDED OR MODIFIED.
  # 1 STAUS DASHBOARD/VIEW BUTTON.
    - we have to add a new dashboard status PENDING FROM CUSTOMER,PENDING FROM FASTECH,PENDING FROM OEM. other dashboard card remains the same.
    - this funality should be recieved from the VIEW button so admin click on the view button for the particular request then there should be another status dropdown besides the previious status this status dropdown should containt the NEW STATUS (PENDING FROM CUSOMTER,OENDING FROM FASTECH, PENDING FROM OEM). status slected should relfect on the dashboard card and also in the SUPPORT REQUEST TABLE.
    - ASSIGNED TEAM should be ASSIGNED TO and the inout would be a name by the admin 
   # 2 TABLE (SUPPORT REQUEST TABLE).
    -  add the pending from column to display the updated statuses.
   # 3 CSV EXPORT BUTTOM.
    - this csv export button should contain whole fields from hte view button exactly like the customer dashboard.
   # 4 MAIL SYSTEM.
    - just like the MAIN dashboard this SUPPORT dasboard should also have the APPROVE AND DIAPPORVE funtion for the VIEW request detail panel when on APPROVE an email is sent to the customer and on DISAPPROVE reuest also the mail is sent with the reason just like hte main dashboard behaviour. 


  ## FOCUS REQUIREMENT DONT DISRUPPT ANYTHING ELSE THIS REQUIREMENTS ARE WAY SIMILLAR LIKE THE PREVIOUS ONE FOR THE MAIN DASHBOARD SO THAT CONTEXT SHOULD HELP.
  ## EVERY FUNTIONALITY SHOULD BE FLEXIBLE AND SHOULD NOT BREAK ANY LOGIC 